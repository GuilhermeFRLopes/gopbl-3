package modelo

// Estrutura padrão para Veículo
// Pode ser usada em scripts Go para interação com o contrato

type Veiculo struct {
	ID        string  // Identificador único (ex: endereço Ethereum)
	Latitude  float64 // Última latitude conhecida
	Longitude float64 // Última longitude conhecida
}
